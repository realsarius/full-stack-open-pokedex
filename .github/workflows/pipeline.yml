name: Deployment pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
    types:
      - opened
      - synchronize

jobs:
  simple_deployment_pipeline:
    runs-on: ubuntu-22.04
    steps:
      - name: Checkout code (run actions/checkout@v3)
        uses: actions/checkout@v3

      - name: Setup Node.js (run actions/setup-node@v3)
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: npm install
        run: npm install

      - name: lint
        run: npx eslint './**/*.{js,jsx}' --fix

      - name: build
        run: npm run build

      - name: unit
        run: npm test

  tag_release:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-22.04
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.merge_commit_sha }}
          fetch-depth: '0'

      - name: Bump version and push tag
        uses: anothrNick/github-tag-action@1.71.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          WITH_V: true
          PRERELEASE: true
          DEFAULT_BUMP: patch
#
#  tag_release:
#    needs: [ simple_deployment_pipeline ]
#    runs-on: ubuntu-22.04
#    if: github.event_name == 'push' && github.ref == 'refs/heads/main' # Ensure it's triggered on push to main
#    steps:
#      - uses: actions/checkout@v4
#        with:
#          fetch-depth: '0'
#
#      - name: Bump version and push tag
#        uses: anothrNick/github-tag-action@1.71.0
#        env:
#          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
#          DEFAULT_BUMP: patch
